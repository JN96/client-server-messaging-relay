# client-server-messaging-relay

## Package Organisation
The project's packages are set up using a "package by feature":
 - com.relay.protocol (The Wire Format)
 - com.relay.state (The Shared State & Limits)
